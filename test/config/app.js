describe('App config', function() {
  it('should define app object');
  it('should define app.imagePath property');
  it('should access non-empty env.ICOC_IMAGE_PATH variable');
  it('should assign app.imagePath to be equal to env.ICOC_IMAGE_PATH');
});