describe('Database config', function() {
  it('should define database object');
  it('should define database.url property');
  it('should have non-empty env.ICOC_MONGODB_DB_URL variable');
  it('database.url should be equal to env.ICOC_MONGODB_DB_URL');
});